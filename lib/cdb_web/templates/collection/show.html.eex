<nav>
  <ul>
    <%= for collection <- @collections do %>
      <li>
        <%= link collection.title, to: collection_path(@conn, :show, collection.slug) %>
      </li>
    <% end %>
  </ul>
</nav>
<article>
  <h2><%= @collection.title %></h2>
  <%= for block <- @collection_body["data"] do %>
    <div>
      <%= render_sir_trevor_block(block) %>
    </div>
  <% end %>
</article>